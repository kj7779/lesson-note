<form method="post" action="{{ path('app_pose_delete', {'id': pose.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pose.id) }}">
    <button class="btn">Delete</button>
</form>
